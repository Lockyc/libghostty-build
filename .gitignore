# Build outputs / working dirs (CI-only; never committed)
/ghostty-src/
/dist/
*.xcframework
*.xcframework.zip
*.sha256
.zig-cache/
zig-out/

# macOS
.DS_Store

# Agent worktrees (house convention)
.claude/worktrees/
